/**
 * name: Quote request
 * subject: Price and availability
 */

return container(`
  ${heading(`
    Quote request
  `)}

  ${paragraph(`
    Please provide a price and availability for the following items:
  `)}

  ${list([""])}

  ${paragraph(`
    Thank you.
  `)}
`);
